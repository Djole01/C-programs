#include <stdio.h>

/* count blanks, tabs and newlines */
int main()
{
	int c;
	int previousChar;
	
	while (previousChar = getchar()) != EOF) {
		previousChar = c;
		putchar(c);
	}
}



// ctrl-d == EOF
